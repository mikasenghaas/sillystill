data_dir: ${paths.data_dir}
batch_size: 8
train_val_test_split: [0.8, 0.1, 0.1]
num_workers: 1
pin_memory: False

dataset:
  patch_size: 128

  augment:
    flip: False
    rotate: False
    blur: False
    brightness: False