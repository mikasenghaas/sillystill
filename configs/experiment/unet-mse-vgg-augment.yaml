# @package _global_

# To run: `python train.py -m experiment={filename}`

defaults:
  - override /data: paired
  - override /model: translation
  - override /model/net: unet
  - override /model/loss: mse-vgg
  - override /logger: wandb

group: unet-mse-vgg-augment
tags: ["unet", "mse-vgg", "augment"]
seed: 42

model:
  optimizer:
    lr: 1e-3 # From unet-mse-vgg
    weight_decay: 1e-3
  scheduler:
    factor: 1e-1
    patience: 5
  net:
    hidden_channels: [64, 128] # From unet-mse-vgg
  loss:
    mse_weight: 1.0 # From unet-mse-vgg
  training_patch_size: 256 # From unet-mse-vgg

hydra:
  sweeper:
    params:
      model.augment: 0, 0.25, 0.5, 0.75, 1